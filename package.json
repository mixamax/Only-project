{
    "name": "only-pr",
    "private": true,
    "version": "0.0.0",
    "type": "commonjs",
    "browserslist": [
        "cover 99%"
    ],
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "preview": "vite preview",
        "pre-deploy": "npm run build ",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.15",
        "vite": "^4.4.5"
    },
    "dependencies": {
        "gh-pages": "^6.0.0"
    }
}
